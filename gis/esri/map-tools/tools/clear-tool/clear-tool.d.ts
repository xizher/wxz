import { $Map, $View } from '../../../web-map/web-map'
import { BaseTool } from '../../base-tool/base-tool'
export declare class ClearTool extends BaseTool<any> {
  constructor (map: $Map, view: $View)
}